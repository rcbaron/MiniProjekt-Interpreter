package ast;

/**
 * Abstrakte Basisklasse fuer alle Ausdruecke (Expressions).
 * Expressions werten zu einem Ergebnis aus (z.B. eine Zahl, ein Boolean
 * oder ein Objekt). Beispiele: 1 + 2, a == b, new A().
 *
 */
public abstract class Expr extends ASTNode {}
